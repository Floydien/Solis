#pragma once
/*
class BoundingVolume {
public:
	/// Check if this bounding volume intersects with another one
	virtual bool intersects(const BoundingVolume &) const;

	/// Check if this bounding volume contains another one
	virtual bool contains(const BoundingVolume &) const;
	
};*/